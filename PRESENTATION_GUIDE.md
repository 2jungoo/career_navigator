# Career-Path Navigator — 발표 가이드

> 이 문서는 프로젝트를 처음 설명하는 사람이 읽어도 이해할 수 있도록 작성했습니다.

---

## 1. 한 줄 요약

> 자기소개서나 CV를 올리면, NLP 알고리즘이 이 사람이 어떤 직무·분야에 맞는지 분석하고 직무와 연구실을 추천해주는 웹 데모입니다.

---

## 2. 왜 이 프로젝트를 만들었나 (문제 정의)

취업·진학 준비생이 자소서를 쓸 때 가장 큰 고민은 이것입니다.

- **"내가 어떤 직무·분야에 맞는 사람인가?"** — 본인도 잘 모르는 경우가 많음
- **"내 경험에 맞는 직무와 연구실은 어디인가?"** — 탐색 비용이 큼

이 앱은 자소서 텍스트를 NLP로 분석해 적합 직무 도메인을 추정하고, 그 근거와 함께 직무·연구실을 추천합니다.

---

## 3. 전체 흐름 (파이프라인)

```
[사용자] PDF / TXT 파일 업로드
            ↓
    [1단계] 텍스트 추출
            ↓
    [2단계] 한국어 문장 분리 + 정규화
            ↓
    [3단계] 분류 — 3가지 방법 중 선택
        ① 키워드 점수 (Baseline)
        ② Bag-of-N-grams 코사인 (BoN)
        ③ SBERT 코사인 (실시간 Transformers.js)
            ↓
    [4단계] 직무 추천 + 연구실 추천
```

---

## 4. 분류 알고리즘 — 3가지를 비교한 이유

핵심 질문: "자소서가 어떤 직무 도메인에 가장 가까운가?"

16개 도메인을 구분합니다:
공학 / 데이터 / 기획 / 연구 / 관리 / 행정 / 경제·금융 / 경영 / 간호·보건 / 자연과학 / 인문사회 / 예술·디자인 / 제조·생산 / 에너지·발전 / 항공·우주 / 미디어·콘텐츠

---

### 방법 1 — 키워드 분류기 (Baseline)

**어떻게 동작하나:**

600개 이상의 도메인별 단어 사전을 미리 만들어놓고, 자소서에 그 단어가 몇 번 나오는지 셉니다.

예시:
```
"React와 TypeScript로 프론트엔드 구현"
→ 공학 core 키워드 'React', 'TypeScript', '프론트엔드' 히트
→ 공학 점수 +3×3 = 9점
```

**구조:**
- `core` 단어: 1개 히트 = 3점 (도메인 고유 단어)
- `general` 단어: 1개 히트 = 1점 (공통적으로 쓰이는 단어)

**한계:** 단어 자체의 의미는 모릅니다. "논문"이라는 단어가 있으면 항상 연구 점수가 올라가지만, 맥락은 무시합니다.

**성능:** Accuracy 86.9% (합성 평가셋 84개)

---

### 방법 2 — Bag-of-N-grams 코사인 유사도 (BoN)

**어떻게 동작하나:**

각 도메인에 대한 설명 프로파일 텍스트를 만들어놓고, 자소서와 프로파일 사이의 "단어 분포 유사도"를 코사인으로 계산합니다.

```
예시: 공학 프로파일 = "소프트웨어 개발 백엔드 API 서버 클라우드..."
자소서 = "React TypeScript 프론트엔드 구현..."

→ 두 텍스트를 벡터로 변환 (나타난 단어/n-gram 빈도)
→ 코사인 유사도 계산 = 벡터 사이 각도의 코사인값
  (1에 가까울수록 유사, 0에 가까울수록 다름)
```

N-gram: 한국어 단어를 2~3글자로 쪼개 겹쳐서 비교합니다. 예를 들어 "구현"이 없어도 "구현하고" "구현했"처럼 변형된 형태도 잡습니다.

**한계:** 어휘 겹침만 봅니다. "의사가 환자를 치료했다"와 "의사(意思)를 전달했다"를 구분 못합니다.

**성능:** Accuracy 89.3% (키워드 45% + BoN 55% 합산, 합성 평가셋 84개)

---

### 방법 3 — SBERT 코사인 유사도 (최고 성능)

**어떻게 동작하나:**

`paraphrase-multilingual-MiniLM-L12-v2` 모델을 사용합니다. 이 모델은 50개 언어로 학습된 Sentence-BERT로, 문장을 384차원 실수 벡터로 변환합니다.

단순한 단어 빈도 벡터가 아닌, **의미**를 숫자로 인코딩한 벡터입니다.

```
예시:
"팀원들과 협력해 서비스를 배포했다" → [0.02, -0.05, 0.31, ... 384개 숫자]
"공학 도메인 프로파일 텍스트"       → [0.01, -0.04, 0.29, ... 384개 숫자]
→ 코사인 유사도 = 0.49  (꽤 가까움)

vs.

"인터뷰 자료와 문화 텍스트를 분석" → [0.08, 0.12, -0.15, ...]
→ 코사인 유사도 = 0.27  (거리 있음)
```

**왜 사전 계산 + 라이브 추론을 병행하나:**

도메인 프로파일·평가셋·PDF 임베딩은 Python으로 **한 번** 사전 계산해 `embeddings.json`(약 1.0MB, 130개 벡터)에 저장하고, 브라우저는 코사인만 계산합니다. 사용자가 새로 올린 문서는 Transformers.js로 브라우저에서 실시간 SBERT 추론(ONNX 양자화 ~40MB)을 수행합니다.

**성능:** Accuracy 90.5% (키워드 45% + SBERT 55% 합산, 합성 평가셋 84개)

---

### 세 방법을 합친 이유 — Hybrid 공식

```
최종 점수 = 키워드 점수(정규화) × 0.45
           + SBERT 코사인(정규화) × 0.55
```

**왜 합치나:**

| 상황 | 키워드의 강점 | SBERT의 강점 |
|---|---|---|
| "Python, Docker, CI/CD 구현" | 정확하게 공학 키워드 탐지 | 맥락 파악 |
| "시스템을 안정적으로 운영했다" | 키워드 약함 | 공학 프로파일과 의미적으로 가까움 |
| "의사결정에 참여했다" | 여러 도메인에 걸침 | 전체 문맥으로 구분 |

SBERT만 쓰면 78.6% (키워드 86.9%보다 낮음), 합치면 90.5%. 둘이 서로의 약점을 보완합니다.

---

## 5. 근거 부족 처리 (Insufficient Evidence)

키워드 점수 합이 너무 낮으면 억지로 도메인을 고르지 않고 "근거 부족"으로 처리합니다.

```
예시: "저는 주말에 산책과 독서를 좋아합니다"
→ 어떤 도메인 키워드도 없음 → 총 점수 0
→ threshold(2) 미달 → primaryDomain = 'insufficient'
→ 직무 추천 안 함
```

평가 결과, 4개 '근거 부족' 샘플을 모두 정확히 기각했습니다 (Coverage 91.7%).

---

## 6. 평가 — Scoring Ablation (95% 부트스트랩 CI 포함)

84개 라벨링 문장으로 여러 방법을 동일 조건에서 비교했습니다. 95% CI는 부트스트랩 복원추출 2,000회로 산출합니다.

| # | 방법 | 임베딩 | Accuracy | Acc 95% CI | Macro F1 | Coverage |
|---|---|---|---|---|---|---|
| 1 | Keyword-only | 없음 | 86.9% | [79.8, 94.0] | 87.4% | 89.3% |
| 2 | BoN Hybrid | BoN | 89.3% | [82.1, 95.2] | 89.4% | 91.7% |
| 3 | SBERT-only | SBERT | 78.6% | [69.0, 86.9] | 79.5% | 89.3% |
| **4** | **SBERT Hybrid** | **SBERT** | **90.5%** | **[83.3, 96.4]** | **90.7%** | **91.7%** |
| 5 | SBERT Hybrid + Threshold | SBERT | 90.5% | [84.5, 96.4] | 90.7% | 91.7% |
| 6 | SBERT + Learned Head (nested LOOCV) | Learned Head | 79.8% | [71.4, 88.1] | 78.6% | 100% |
| 7 | Learned Head Hybrid (참고) | Learned Head | 92.9% | [86.9, 97.6] | 89.7% | 100% |

> **평가셋을 68→84개로 키우며 의미로만 판단하는 어려운 문장(lexical_gap)·도메인 혼재(stress_test)를 대거 추가**했습니다. 그 결과 SBERT Hybrid가 97.1%→90.5%로 내려갔는데, 이는 성능 하락이 아니라 **기존 97.1%가 쉬운 합성셋의 과대평가였음**을 의미합니다 — 더 어렵고 정직한 평가셋의 수치입니다. 반대로 6번 학습 헤드는 균형화 덕에 76.5%→**79.8%**로 올랐습니다. 서비스 기본값은 항상 4번 코사인 Hybrid이므로 성능에 영향 없습니다.

**핵심 인사이트:**

SBERT 단독(78.6%)이 키워드 단독(86.9%)보다 낮습니다. 이유는 두 가지입니다.

1. MiniLM은 다국어 범용 모델이라 한국어 자소서 특화 표현에 약합니다
2. 도메인별 핵심 키워드("Docker", "간호", "법령")는 키워드 사전이 정확하게 잡습니다

하지만 합치면 90.5%. 서로 잡는 영역이 다르기 때문입니다.

---

## 6-1. 학습 헤드 고도화 + 학습곡선 (Learning Curve)

SBERT 임베딩 위에 **학습하는** 분류 헤드(Logistic Regression)를 추가했습니다. 단순 코사인이 아니라 *지도학습* 요소입니다.

**고도화 3종** (소규모·불균형 데이터 대응):
1. **표준화**(차원별 평균/표준편차 정규화)
2. **클래스 가중**(`balanced` — 클래스당 5개 불균형 보정)
3. **누수 없는 C 선택**(nested CV — 외부 LOOCV, 내부에서 정규화 강도 C를 독립 선택)

→ 이 세 가지로 LOOCV가 **42.6% → 76.5%**(Macro F1 0.349 → 0.745)로 올랐고, 이어 **평가셋 균형화(68→84개)로 79.8%**(Macro F1 0.786)까지 향상됐습니다.

**알고리즘 비교 (LogReg vs MLP):** 같은 LOOCV로 비선형 헤드도 실험했습니다 — **MLP(은닉층 128) 67.9% < LogReg 79.8%**. 클래스당 5개의 소규모 데이터에서는 선형 헤드가 우수하며(MLP 과적합), 배포는 LogReg로 유지합니다(브라우저 선형 추론과도 호환).

**실데이터 투입 실험 (정직한 음성 결과):** 실제 자소서 PDF를 학습에 추가하니 평가셋 정확도가 79.8%→**77.4%로 하락**했습니다(PDF 항목 자체 33%). 합성↔실 분포 차이 때문이며, 스크립트가 이를 감지해 자동으로 eval-only 배포를 선택합니다. "데이터를 무작정 늘리는 것이 답이 아니다"를 실험으로 보여주는 사례입니다.

**학습곡선 — "왜 아직 코사인보다 낮은가"를 실험으로 증명:**

학습 데이터를 n=14에서 56까지 늘리며 성능을 측정했습니다.

| 학습 샘플 수 | Accuracy | Macro F1 |
|---|---|---|
| 14 | 41.7% | 0.322 |
| 28 | 66.7% | 0.622 |
| 42 | 71.4% | 0.660 |
| 56 | 76.2% | 0.732 |

**핵심:** 곡선이 **상승하며 고원에 도달하지 않습니다.** 즉 헤드의 한계는 *알고리즘*이 아니라 **데이터 양**입니다. (질 좋은) 데이터를 더 모으면 코사인(90.5%)에 더 가까워질 가능성을 정량적으로 보여줍니다. (Classifier Test 탭에 코사인·랜덤 기준선과 함께 차트로 표시)

---

## 8. 오분류 분석 (Error Analysis)

합성 평가셋 오분류 2건 모두 **어휘적 모호성** 때문입니다.

| ID | 정답 | 예측 | 원인 |
|---|---|---|---|
| human-3 | 인문사회 | 연구 | "학술지에 **논문**을 투고"에서 '논문' 키워드가 연구 도메인을 올림 |
| art-3 | 예술·디자인 | 경영 | "**브랜드** 아이덴티티 디자인"에서 '브랜드' 키워드가 경영 도메인을 올림 |

이것은 **NLP의 어휘 중의성(lexical ambiguity) 문제**입니다. "논문"은 연구자도 쓰고 인문학자도 씁니다. 단어 하나만으로는 맥락을 완전히 구분하기 어렵습니다. 한국어 특화 SBERT (Korean SBERT, bge-m3-ko)였다면 개선될 가능성이 있습니다.

---

## 8-1. 실데이터 홀드아웃 (Real-Data Holdout)

합성 평가셋은 짧고 단순합니다. **실제 자소서 PDF 30개**(평균 약 2,400자, 다중 페이지) 중 파일명으로 직군 라벨을 부여할 수 있는 29개(10개 도메인)로 일반화 성능을 별도 측정했습니다.

| 방법 | 임베딩 | Accuracy | Acc 95% CI | Macro F1 |
|---|---|---|---|---|
| Keyword-only | 없음 | 69.0% | [51.7, 86.2] | 79.0% |
| BoN Hybrid | BoN | 72.4% | [55.2, 86.2] | 85.6% |
| **SBERT Hybrid** | **SBERT** | **72.4%** | **[55.2, 86.2]** | **86.4%** |

**핵심 인사이트:** 합성셋 97% → 실데이터 72%로 정확도가 떨어집니다. 실제 자소서는 도메인이 혼재되고 표현이 다양하기 때문이며, 이는 모델의 일반화 한계를 **정직하게 드러내는** 결과입니다. 동시에 실데이터에서는 Macro F1 기준 SBERT Hybrid(86.4%)가 Keyword(79.0%)를 더 뚜렷이 앞서, 의미 임베딩의 가치가 합성셋보다 오히려 분명해집니다.

---

## 9. 데이터 설명

### 평가 데이터셋 (84개, 16개 직무 도메인 각 5개 균형)

| 종류 | 개수 | 설명 |
|---|---|---|
| synthetic_resume | 61개 | 도메인별 명확한 합성 자소서 문장 |
| lexical_gap | 11개 | 키워드 없이 의미만으로 판단해야 하는 문장 |
| stress_test | 8개 | 두 도메인이 섞인 어려운 문장 |
| negative_control | 4개 | "주말에 산책을 했다" 같은 무관한 문장 |

> 평가셋은 `src/lib/evaluationDataset.js` 한 곳에서만 관리하고 `node scripts/exportEvalDataset.mjs`로 JSON을 자동 생성합니다(앱·Python 학습 간 불일치 방지).

### PDF 실제 자소서 (30개)

실제 채용·대학원 합격 자소서 PDF 30건에서 텍스트 추출 후 SBERT 임베딩 사전계산.

---

## 10. 사전 계산 데이터 파일

Python 스크립트를 한 번 실행해서 만든 파일들입니다. 이후에는 브라우저가 이 파일만 불러씁니다.

| 파일 | 크기 | 내용 |
|---|---|---|
| `public/data/embeddings.json` | ~1.0MB | SBERT 벡터 130개 (도메인 16개 + 평가셋 84개 + PDF 30개) |
| `public/data/pdf_corpus.json` | - | PDF에서 추출한 텍스트 |

---

## 11. 기술 스택

| 분류 | 기술 |
|---|---|
| 프론트엔드 | React (Vite), Tailwind CSS |
| NLP 모델 | sentence-transformers (MiniLM-L12) + Transformers.js (브라우저 실시간) |
| PDF 파싱 | pdfjs-dist |
| 사전 계산 | Python, sentence-transformers |
| 시각화 | Recharts, ReactFlow |

---

## 12. 예상 Q&A

**Q. SBERT가 뭔가요?**

Sentence-BERT의 약자입니다. 기존 BERT가 단어 단위 임베딩을 만들었다면, SBERT는 문장 전체를 하나의 고정된 크기 벡터로 만들어줍니다. 의미가 비슷한 문장은 벡터 사이 각도가 작고(코사인 값이 1에 가깝고), 다른 문장은 각도가 큽니다.

---

**Q. 왜 SBERT 단독이 키워드보다 성능이 낮나요?**

MiniLM은 50개 언어 범용 모델입니다. "Docker", "Kubernetes" 같은 도메인 특화 기술 용어는 키워드 사전이 훨씬 정확하게 잡습니다. SBERT는 의미적 유사성을 잘 잡지만, 이 데이터셋처럼 도메인 전문 용어가 강한 경우에는 키워드 방식이 더 직접적입니다.

---

**Q. 실시간 SBERT는 어떻게 동작하나요?**

Transformers.js를 사용해 브라우저에서 직접 SBERT 모델을 실행합니다. ONNX 양자화 모델(~40MB)을 HuggingFace CDN에서 받아 캐시하며, Python 사전계산과 동일한 모델(`paraphrase-multilingual-MiniLM-L12-v2`) + mean pooling + L2 정규화를 적용해 384차원 벡터를 생성합니다. 모델 로딩에 처음 몇 초 걸리며, 실패하면 BoN 코사인으로 자동 폴백합니다.

---

**Q. 16개 도메인은 어떻게 정했나요?**

실제 채용 공고와 대학원 진학 경로를 기준으로 정했습니다. 처음 12개에서 시작해 실제 자소서 PDF 30개를 분석한 결과 제조·생산 / 에너지·발전 / 항공·우주 / 미디어·콘텐츠가 독립 도메인으로 필요하다고 판단해 16개로 확장했습니다.

---

**Q. 한계가 뭔가요?**

1. **SBERT 모델 크기**: 첫 실행 시 ~40MB 다운로드. 이후 브라우저 캐시.
2. **평가셋 규모**: 84개는 수업 시연용. 부트스트랩 95% CI를 함께 보고해 통계적 불확실성을 투명하게 공개.
3. **어휘 중의성**: "브랜드", "논문" 같은 단어가 여러 도메인에 걸침 (Error Analysis에서 2건 분석).
4. **합성↔실데이터 갭**: 합성 평가셋 97% → 실제 자소서 PDF 72%. 실데이터 홀드아웃으로 일반화 한계를 투명하게 공개.

---

## 13. 발표 1분 핵심 요약

> "자소서 텍스트를 입력하면 16개 직무 도메인 중 어디에 맞는지 분류하고 직무와 연구실을 추천하는 시스템입니다.
>
> 분류는 키워드 사전 + SBERT 임베딩을 45:55로 합산하는 Hybrid 방식을 씁니다. 새 업로드 문서는 Transformers.js로 브라우저에서 실시간 SBERT 추론을 실행하며, 84개 라벨링 합성 평가셋 ablation(키워드/BoN/SBERT/학습헤드) + 부트스트랩 95% CI에 더해, 실제 자소서 PDF 29개 홀드아웃으로 일반화 성능까지 측정했습니다.
>
> 가중치 민감도 분석에서 45:55 선택이 성능 고원(plateau) 구간에 있음을 실험으로 확인했습니다. 모든 점수가 어떤 공식에서 나오는지 추적 가능합니다."
